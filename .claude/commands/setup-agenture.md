---
description: Phase 2 — install agent skills from skills.sh using full repo context
---

Read **`AGENT_SKILLS_INSTALL.md`** at the repository root and execute phase 2 as described there — run the actual shell commands from the project root (network allowed). Use `--agent claude-code` (or `-a claude-code`) on every `npx skills add`. End with `npx skills list` and tell the user what to commit.
